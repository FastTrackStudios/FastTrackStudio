[package]
name = "task-app-web"
version = "0.1.0"
edition = "2021"
authors = ["Cody Wright"]
license = "MIT OR Apache-2.0"

[dependencies]
dioxus = { version = "0.7.9", default-features = false, features = ["lib", "web", "launch"] }
web-sys = { version = "0.3.98", features = ["Window", "Location", "History"] }
task-ui = { path = "../../crates/task-ui" }
