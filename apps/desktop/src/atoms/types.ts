// Re-export types from the old hook for compatibility
export type {
  SetlistMessage,
  Setlist,
  Song,
  Section,
  Marker,
  Position,
  MusicalPosition,
  TransportState,
  TransportStates,
} from '../hooks/use-websocket';

