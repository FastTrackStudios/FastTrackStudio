[package]
name = "task-app-desktop"
version = "0.1.0"
edition = "2021"
authors = ["Cody Wright"]
license = "MIT OR Apache-2.0"

[dependencies]
dioxus = { version = "0.7.6", default-features = false, features = ["lib", "desktop"] }
task-ui = { path = "../../crates/task-ui" }
