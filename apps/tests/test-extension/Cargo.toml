[package]
name = "test-extension"
version = "0.1.0"
edition = "2021"
description = "Test host extension that loads daw-standalone for integration testing"

[[bin]]
name = "test-extension"
path = "src/main.rs"

[dependencies]
# Host runtime infrastructure
host-runtime.workspace = true

# Protocols (for spawning cells)
daw-proto.workspace = true
session-proto.workspace = true
gateway-proto.workspace = true

# Runtime
tokio.workspace = true
tracing.workspace = true
