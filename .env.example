# FastTrackStudio Environment Configuration
# Copy this file to .env and update the paths for your system
# 
# cp .env.example .env
# Then edit .env with your REAPER paths

# REAPER resource directory path (where REAPER stores its configuration and plugins)
# On macOS, this is typically: ~/Library/Application Support/REAPER
# Or a custom portable installation path
REAPER_PATH=/Users/codywright/Music/FTS-REAPER

# REAPER executable path (for launching REAPER)
# On macOS, this is typically inside the .app bundle
REAPER_EXECUTABLE=/Users/codywright/Music/FTS-REAPER/FTS-LIVE.app/Contents/MacOS/REAPER

# Build mode: "debug" or "release"
# Can be overridden per-command: BUILD_MODE=release just build-extension
BUILD_MODE=debug


# Extension feature flags (for conditional compilation)
# Options:
#   - "all" or empty: use default features from Cargo.toml (includes "input", "live", "lyrics", "iroh_server")
#   - "none": build with --no-default-features (completely transparent, no features)
#   - comma-separated list: build with specific features, e.g., "input" or "input,live,lyrics"
# Available features:
#   - input: FTS-Input key interception and redirection
#   - live: Live tracks functionality
#   - dev: Dev/debug actions
#   - lyrics: Lyrics functionality
#   - iroh_server: IROH server for IPC with desktop app
# Examples:
#   EXTENSION_FEATURES=all              # Use defaults (input, live, lyrics, iroh_server)
#   EXTENSION_FEATURES=none              # No features (completely transparent)
#   EXTENSION_FEATURES=input              # Only input feature
#   EXTENSION_FEATURES=input,live         # Input and live features
#   EXTENSION_FEATURES=input,lyrics      # Input and lyrics features
EXTENSION_FEATURES=all
