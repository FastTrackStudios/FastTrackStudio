# FastTrackStudio Environment Configuration
# Copy this file to .env and update the paths for your system
# 
# cp .env.example .env
# Then edit .env with your REAPER paths

# REAPER resource directory path (where REAPER stores its configuration and plugins)
# On macOS, this is typically: ~/Library/Application Support/REAPER
# Or a custom portable installation path
REAPER_PATH=/Users/codywright/Music/FTS-REAPER

# REAPER executable path (for launching REAPER)
# On macOS, this is typically inside the .app bundle
REAPER_EXECUTABLE=/Users/codywright/Music/FTS-REAPER/FTS-LIVE.app/Contents/MacOS/REAPER

# Build mode: "debug" or "release"
# Can be overridden per-command: BUILD_MODE=release just build-extension
BUILD_MODE=debug
