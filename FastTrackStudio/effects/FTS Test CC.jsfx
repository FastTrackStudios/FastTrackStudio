desc:FTS Test CC
// Minimal JSFX that sends CC 1 = slider1*127 on bus 0, channel 16 every @block.
// Used for testing plink MIDI routing.

slider1:0.5<0,1>Value

@init
ext_midi_bus = 1;

@block
midi_bus = 15;
midisend(0, $xBF, 1, slider1 * 127);
