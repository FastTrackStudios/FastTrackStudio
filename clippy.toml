# MSRV floor for clippy's version-gated lints. Matches the workspace's
# edition-2024 requirement (rustc >= 1.85); pinned a touch higher to track
# the toolchain in rust-toolchain.toml.
msrv = "1.90"
