//! Director service context for dependency injection

use crate::ui::signals::{LocalDirectorClient, MockDirector};

crate::define_service_context! {
    /// Director service context for dependency injection
    name: Director,
    client_type: LocalDirectorClient<MockDirector>,
}
