[package]
name = "visibility-manager"
version = "0.1.0"
edition = "2024"
description = "Dynamic track visibility management using instrument classification"
license = "MIT"

[dependencies]
dynamic-template = { path = "../dynamic-template" }
daw = { path = "../daw" }
thiserror = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
pretty_assertions = "1"
