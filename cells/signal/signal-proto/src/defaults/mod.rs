//! Default configurations — pre-built blocks and instrument-specific rigs.
//!
//! These are optional starter configurations. Rigs can be built from scratch,
//! but these provide sensible defaults and reference implementations.

pub mod blocks;
pub mod modules;

pub mod templates;
