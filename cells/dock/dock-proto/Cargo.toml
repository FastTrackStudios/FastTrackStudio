[package]
name = "dock-proto"
description = "Dock protocol — domain types for modular layout/docking system"
version.workspace = true
edition.workspace = true

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
uuid = { version = "1.0", features = ["v4", "serde"] }

[lints]
workspace = true
