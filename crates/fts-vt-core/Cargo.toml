[package]
name = "fts-vt-core"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Core types for fts-visual-testing: stories, knobs, interactions, registry."

[dependencies]
linkme = { workspace = true }
serde = { workspace = true, optional = true }

[features]
default = []
serde = ["dep:serde"]

[lints]
workspace = true
