[package]
name = "fts-modulation"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "MSEG pattern engine with tempo sync, triggers, and envelope followers"

[dependencies]
audiocore-dsp = { workspace = true, default-features = false }
