# Obsidian Integration

Obsidian plugin packaging and vault-specific behavior belongs here.

Shared markdown parsing and core task behavior stay in `crates/task-core`.

- `plugin/` contains the Obsidian plugin package and WASM crate.
