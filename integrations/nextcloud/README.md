# Nextcloud Integration

Provider-specific Nextcloud work belongs here: CalDAV, Deck, WebDAV, file sync,
account discovery, and webhook or polling orchestration.

Shared task/calendar semantics stay in `crates/task-core`.
