---
title: tasks
type: folder
tags: [tasks-folder]
---

# tasks

Task notes — markdown files with TaskNotes-shape frontmatter (`type: task`). Captured via `cargo run -p task-cli -- task capture "…"`.
