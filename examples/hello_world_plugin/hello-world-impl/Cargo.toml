[package]
name = "hello-world-impl"
version = "0.1.0"
edition = "2024"

[lib]
# CRITICAL: Must be dylib for hot-reloading!
crate-type = ["rlib", "dylib"]

[dependencies]
hello-world-services = { path = "../services" }
roam = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
uuid = { workspace = true }

# For thread-safe state
parking_lot = "0.12"
