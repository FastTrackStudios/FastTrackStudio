[package]
name = "midi-tools"
version.workspace = true
edition.workspace = true
description = "midi-tools — MIDI note manipulation as pure arithmetic (velocity shaping; notes in, edits out)"

[dependencies]
# The only dependency, and only for `velocity::Randomize`. Everything
# else here is arithmetic — if this list grows a DAW, a UI toolkit or an
# async runtime, the thing being added belongs in midi-tools-daw or
# midi-tools-ui instead.
rand = { workspace = true }
# Only for the no-DAW `DemoSink`'s diagnostics.
tracing = { workspace = true }

[lints]
workspace = true
