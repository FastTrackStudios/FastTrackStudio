[package]
name = "email-store"
description = "Local cache for the email feature: Maildir on disk + SQLite FTS5 index. Sits in front of one-or-more `email_proto::EmailSync` backends and re-exposes the same trait, so clients can talk to the cache the same way they'd talk to a live server."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
email-proto = { workspace = true }
architect = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }
