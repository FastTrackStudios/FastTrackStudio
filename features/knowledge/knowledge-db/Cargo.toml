[package]
name = "knowledge-db"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true

# SeaORM-side concerns for the `knowledge` feature.
#
# Generic CRDT migrations only — no projection tables in Phase A.
# Add SQL-shaped projections here when graph / backlink queries
# outgrow the in-memory Loro traversal (currently fine for the
# expected vault sizes).

[dependencies]
knowledge-proto.workspace = true
crdt-seaorm.workspace = true
sea-orm.workspace = true
sea-orm-migration.workspace = true
async-trait = "0.1"
